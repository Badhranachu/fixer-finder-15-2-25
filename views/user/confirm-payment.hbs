<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Technician Slot</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      background-color: #121212;
      color: #fff;
      font-family: 'Poppins', sans-serif;
    }
    .container {
      max-width: 800px;
    }
    .glass-card {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      padding: 20px;
      box-shadow: 0px 4px 10px rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      transition: transform 0.3s ease-in-out;
    }
    .glass-card:hover {
      transform: scale(1.02);
    }
    .btn-primary, .btn-success {
      border-radius: 25px;
      padding: 12px 25px;
      font-weight: bold;
    }
    .btn-primary:hover {
      background-color: #0d6efd;
    }
    .btn-success:hover {
      background-color: #198754;
    }
    .product-img {
      max-height: 180px;
      border-radius: 10px;
    }
  </style>
</head>
<body>
  <div class="container mt-5 text-center">
    <h2 class="fw-bold text-warning">Confirm Your Technician Slot</h2>
    <div class="glass-card mt-4">
      <h4 class="text-light">Technician: <span class="text-warning">{{product.name}}</span></h4>
      <img src="{{product.image}}" alt="{{product.name}}" class="img-fluid product-img mb-3">
      <p><strong>Fair:</strong> ₹{{totalPrice}}</p>
      <p><strong>Payment Method:</strong> {{paymentMethod}}</p>
      <p><strong>Slot:</strong> {{quantity}}</p>
      <form action="/confirmed" method="POST">
        <input type="hidden" name="productId" value="{{product.id}}">
        <input type="hidden" name="totalPrice" value="{{totalPrice}}">
        <input type="hidden" name="quantity" value="{{quantity}}">
        <input type="hidden" name="paymentMethod" value="{{paymentMethod}}">
        <button type="submit" class="btn btn-success mt-3">Confirm Slot</button>
      </form>
    </div>
    {{!-- <div class="mt-4">
      <a href="/" class="btn btn-outline-primary">Home</a>
      <a href="/orders" class="btn btn-outline-secondary">Previous Bookings</a>
    </div> --}}
  </div>
</body>
</html>