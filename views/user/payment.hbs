<div class="container mt-5">
  <h2 class="text-center mb-4"> Payment Method</h2>

  {{#if isMultiple}}
  <div class="text-center mb-4">
    <h4 class="text-success"><strong>Total Price:</strong> ₹{{totalPrice}}</h4>
  </div>
  <div class="table-responsive">
    <table class="table table-bordered text-center">
      <thead>
        <tr>
          <th>Product Name</th>
          <th>Price</th>
          <th>Quantity</th>
          <th>Total</th>
        </tr>
      </thead>
      <tbody>
        {{#each cartDetails}}
        <tr>
          <td>{{this.name}}</td>
          <td>₹{{this.price}}</td>
          <td>{{this.quantity}}</td>
          <td>₹{{this.total}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{else}}
  <div class="text-center mb-4">
    <h4 class="text-primary"><strong>Technician:</strong> {{product.name}}</h4>
    <h4 class="text-success"><strong>Fair:</strong> ₹{{totalPrice}}</h4>
  </div>
  {{/if}}

  <div class="row justify-content-center">
    <div class="col-md-4 mb-4">
      <div class="card shadow-sm">
        <div class="card-body text-center">
          <h4 class="card-title">Cash on Delivery (COD)</h4>
          <form action="/confirm-payment" method="POST">
            <input type="hidden" name="paymentMethod" value="COD" />
            {{#if isMultiple}}
            <input type="hidden" name="cartDetails" value='{{json cartDetails}}' />
            <input type="hidden" name="totalPrice" value="{{totalPrice}}" />
            {{else}}
            <input type="hidden" name="productName" value="{{product.name}}" />
            <input type="hidden" name="productId" value="{{product.id}}" />
            <input type="hidden" name="quantity" value="{{quantity}}" />
            <input type="hidden" name="totalPrice" value="{{totalPrice}}" />
            {{/if}}
            <button type="submit" class="btn btn-success btn-lg">Confirm Booking with COD</button>
          </form>
        </div>
      </div>
    </div>
    {{!-- <div class="col-md-4 mb-4">
      <div class="card shadow-sm">
        <div class="card-body text-center">
          <h4 class="card-title">Online Payment</h4>
          <form action="/finalize-payment" method="POST">
            <input type="hidden" name="paymentMethod" value="Online" />
            {{#if isMultiple}}
            <input type="hidden" name="cartDetails" value='{{json cartDetails}}' />
            <input type="hidden" name="totalPrice" value="{{totalPrice}}" />
            {{else}}
            <input type="hidden" name="productName" value="{{product.name}}" />
            <input type="hidden" name="productId" value="{{product.id}}" />
            <input type="hidden" name="quantity" value="{{quantity}}" />
            <input type="hidden" name="totalPrice" value="{{totalPrice}}" />
            {{/if}}
            <button type="submit" class="btn btn-primary btn-lg">Pay Now</button>
          </form>
        </div>
      </div>
    </div> --}}
  </div>
</div>

<style>
  body {
    background-color: #121212;
    color: #ffffff;
    font-family: 'Poppins', sans-serif;
  }

  .container {
    max-width: 1100px;
    background: linear-gradient(135deg, #1e1e1e, #333333);
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
    height: 400px;
  }

  .card {
    background: #222;
    border-radius: 12px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease;
  }

  .card:hover {
    transform: scale(1.02);
  }

  .card-body {
    text-align: center;
  }

  .btn-success {
    background: linear-gradient(90deg, #00c853, #b2ff59);
    color: #000;
    font-weight: bold;
    border: none;
    padding: 12px;
    border-radius: 8px;
    transition: 0.3s;
  }

  .btn-success:hover {
    background: linear-gradient(90deg, #b2ff59, #00c853);
    transform: scale(1.05);
  }

  .text-primary {
    color: #64ffda !important;
  }

  .text-success {
    color: #b2ff59 !important;
  }

  table {
    background: #1e1e1e;
    color: #ffffff;
    border-radius: 10px;
  }
</style>